<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Overrides MAUI's generated Maui.SplashTheme so the splash icon uses the
         transparent splash_logo drawable instead of the color-baked bitmap,
         keeping the native splash identical to the web splash in both themes. -->
    <style name="Maui.SplashTheme" parent="Maui.MainTheme.NoActionBar">
        <item name="maui_splash">true</item>
        <item name="android:windowBackground">@drawable/splash_icon</item>
        <item name="android:windowSplashScreenBackground">@color/maui_splash_color</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
    </style>
</resources>
